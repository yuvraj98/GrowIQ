// src/services/campaignService.js
// Campaign data service — Sprint 5-8
const logger = require('../utils/logger');

// Placeholder — will be implemented in Sprint 5-8
module.exports = {
    async getCampaigns(agencyId, filters) {
        logger.info('campaignService.getCampaigns — Coming Sprint 5-6');
        return [];
    },

    async upsertCampaignMetrics(clientId, metrics) {
        logger.info('campaignService.upsertCampaignMetrics — Coming Sprint 5-6');
        return null;
    },
};
